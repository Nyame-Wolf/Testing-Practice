class Calculator{
    static add = (x,y) =>  x + y;
    
    static subtract = (x,y) =>  x - y;

     static multiply = (x,y) =>  x * y;

      static divide = (x,y) =>  x / y;
}

module.exports = Calculator